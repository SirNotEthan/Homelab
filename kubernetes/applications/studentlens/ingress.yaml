apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: studentlens
  namespace: studentlens
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - studentlens.net
        - www.studentlens.net
      secretName: studentlens-public-tls
    - hosts:
        - studentlens.sirnotethan.uk
      secretName: studentlens-sirnotethan-tls
  rules:
    - host: studentlens.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: studentlens
                port:
                  number: 80
    - host: studentlens.sirnotethan.uk
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: studentlens
                port:
                  number: 80
    - host: www.studentlens.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: studentlens
                port:
                  number: 80
