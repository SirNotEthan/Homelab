# Kubernetes

Declarative cluster resources are split by responsibility:

- `infrastructure/` for networking, ingress, certificates, and storage;
- `platform/` for shared operational capabilities; and
- `applications/` for user-facing workloads.

See [project standards](../docs/standards.md) before adding resources.
