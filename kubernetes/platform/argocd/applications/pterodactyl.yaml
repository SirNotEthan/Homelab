apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pterodactyl
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SirNotEthan/Homelab.git
    targetRevision: main
    path: kubernetes/applications/pterodactyl
  destination:
    server: https://kubernetes.default.svc
    namespace: pterodactyl
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
