# Ansible

Host configuration and repeatable operating-system changes belong here. Roles
and playbooks must document supported hosts, required variables, validation, and
rollback. Vault passwords and unencrypted secrets must not be committed.
