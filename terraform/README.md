# Terraform

Declarative infrastructure managed through external APIs belongs here. State
files and credentials must not be committed. Every module must document its
backend, providers, inputs, outputs, and safe destruction boundaries.
